music player app.
CRUD FOR SONGS each song has a title artist and duration
Implement a master-detail user interface
Use a REST service to fetch data
Use web sockets to receive server-side notifications